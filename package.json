{
  "name": "ffm-mcp",
  "version": "0.2.0",
  "description": "MCP server cho sea-fulfillment: tự login bằng username/password, gọi /orders/get_orders, normalize order theo các field OrderExportExcel.js dùng để export Excel.",
  "type": "module",
  "bin": {
    "ffm-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc -w",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
